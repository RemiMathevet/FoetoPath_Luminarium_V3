flask>=3.0,<4.0
openslide-python>=1.3
Pillow>=10.0,<12.0
numpy>=1.24,<3.0
blosc2>=2.5,<3.0
# Lecteur MRXS maison (Rust/PyO3) — ingest + masques + integrity.
# Build via maturin (PEP 517) au pip install. Épinglé au commit master faute de tag.
omnissiah @ git+https://github.com/RemiMathevet/omnissiah.git@9e67853b2914
